namespace Sorts
{
    public enum BigOValue
    {
        Constant,    // O(1)
        Logarithmic, // O(log n) 
        Linear,      // O(n)
        LogLinear,   // O(n log n)
        Quadratic,   // O(n^2)
        Cubic,       // O(n^3)
        Exponential  // O(2^n)

    }
}
